<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
<plist version="1.0">
<dict>
	<key>CFBundleDevelopmentRegion</key>
	<string>en</string>
	<key>CFBundleExecutable</key>
	<string>Moomux</string>
	<!-- The Mac app's identifier, not a `.ios` one. App Store Connect ties a
	     record to one bundle id and only carries several platforms under it
	     when they share it, and the id is locked the moment a first build is
	     uploaded — so this is the one chance to leave room for the Mac build
	     joining the same TestFlight record later. -->
	<key>CFBundleIdentifier</key>
	<string>app.moomux.Moomux</string>
	<key>CFBundleInfoDictionaryVersion</key>
	<string>6.0</string>
	<key>CFBundleName</key>
	<string>Moomux</string>
	<key>CFBundlePackageType</key>
	<string>APPL</string>
	<key>CFBundleShortVersionString</key>
	<string>0.0.1</string>
	<!-- From the build setting, so `make testflight` can give every upload the
	     distinct build number App Store Connect insists on. `make ios` has no
	     build settings and substitutes a literal with PlistBuddy. -->
	<key>CFBundleVersion</key>
	<string>$(CURRENT_PROJECT_VERSION)</string>
	<!-- Plain TCP to the core over the tailnet, no custom cryptography. Without
	     this every TestFlight build sits in "Missing Compliance" and is never
	     offered to testers, which reads as the upload having failed. -->
	<key>ITSAppUsesNonExemptEncryption</key>
	<false/>
	<key>CFBundleIcons</key>
	<dict>
		<key>CFBundlePrimaryIcon</key>
		<dict>
			<key>CFBundleIconFiles</key>
			<array>
				<string>AppIcon60x60</string>
			</array>
		</dict>
	</dict>
	<!-- A tailnet 100.x address over utun is not gated, but the host field
	     accepts anything: point it at a LAN address or a .local name and iOS
	     local-network privacy silently fails the connect unless this key is
	     here to put words on the prompt. -->
	<key>NSLocalNetworkUsageDescription</key>
	<string>Moomux connects to the moomux core running on your Mac.</string>
	<key>LSRequiresIPhoneOS</key>
	<true/>
	<key>MinimumOSVersion</key>
	<string>18.0</string>
	<key>UILaunchScreen</key>
	<dict/>
	<key>UISupportedInterfaceOrientations</key>
	<array>
		<string>UIInterfaceOrientationPortrait</string>
		<string>UIInterfaceOrientationLandscapeLeft</string>
		<string>UIInterfaceOrientationLandscapeRight</string>
	</array>
</dict>
</plist>
